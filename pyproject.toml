[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mega-ai"
version = "0.1.0"
description = "Operational-style multi-agent orchestration foundation"
requires-python = ">=3.11"

[tool.setuptools]
packages = []

[tool.pytest.ini_options]
pythonpath = ["."]
